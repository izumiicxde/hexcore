import { ThemeModeToggle } from "@/components/mode-toggler";

const page = () => {
  return (
    <div className="flex h-screen w-screen items-center justify-center">
      <ThemeModeToggle />
    </div>
  );
};

export default page;
