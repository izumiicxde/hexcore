import { ThemeToggle } from "@/components/theme-toggler";

const page = () => {
  return (
    <div>
      <nav className="w-full h-fit overflow-hidden">
        <div className=""></div>
      </nav>
    </div>
  );
};

export default page;
